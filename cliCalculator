class calculator :
    def add (self , a, b):

        result = a + b 
        print(f"Adding {a} + {b} = {result}")
        return result
    
    def sub ( self, a, b):
        result = a-b
        print(f"Subtracting {a} - {b} = {result}")
        return result